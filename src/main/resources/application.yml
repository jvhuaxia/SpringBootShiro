spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demo
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
    mode: LEGACYHTML5

server:
  port: 8090
mybatis:
  type-aliases-package: com.example.demo62.pojo
  mapper-locations: classpath:mapper/**/*.xml
